replicaCount: 1

image:
  repository: ghcr.io/fiyinadedigba/production-platform-api
  tag: v1.0.0
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 3000

containerPort: 3000
